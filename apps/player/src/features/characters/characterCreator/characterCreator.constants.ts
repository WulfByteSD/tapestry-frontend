// Aspect allocation rules for character creation.
// These constants enforce Tapestry's starting ruleset.
// The global per-aspect cap (post-creation) is +4; here it is tightened to +2.

export const STARTING_POINTS = 2;
export const MIN_ASPECT = -2;
export const MAX_ASPECT_AT_CREATION = 2;
export const MAX_PENALTY_ASPECTS = 2; // at most 2 aspects may go to -1
